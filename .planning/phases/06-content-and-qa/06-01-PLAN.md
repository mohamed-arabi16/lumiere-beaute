---
phase: 06-content-and-qa
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/i18n/locales/en/en.json
  - src/i18n/locales/tr/tr.json
  - src/i18n/locales/ar/ar.json
  - src/components/sections/ContactFormSection.tsx
  - src/pages/AboutPage.tsx
  - .env.example
  - .gitignore
autonomous: true
requirements:
  - FNDTN-03
  - FNDTN-04

must_haves:
  truths:
    - "Visiting /contact in Turkish or Arabic shows a translated phone label and phone input field"
    - "Visiting /about in any language shows no raw English dev-stub string"
    - "emailjs.send() passes phone: values.phone as a fifth templateParam"
    - "Zero raw translation keys appear in any locale for the phone field"
    - ".env.example is committed with all four VITE_ env var placeholders documented"
    - ".gitignore covers .env and .env.local so real credentials cannot be committed"
  artifacts:
    - path: "src/components/sections/ContactFormSection.tsx"
      provides: "5-field contact form (name, email, service, phone, message)"
      contains: "phone: values.phone"
    - path: "src/pages/AboutPage.tsx"
      provides: "Coming-soon stub with translated text, no hardcoded English stub"
    - path: ".env.example"
      provides: "Documented env vars for EmailJS + WhatsApp with placeholder values"
  key_links:
    - from: "src/components/sections/ContactFormSection.tsx"
      to: "emailjs.send()"
      via: "templateParams object"
      pattern: "phone: values\\.phone"
    - from: "src/i18n/locales/en/en.json"
      to: "ContactFormSection phone label"
      via: "t('contact.form.phone_label')"
      pattern: "phone_label"
---

<objective>
Add the phone field to the contact form (per CONTEXT.md decision), fix the AboutPage stub, verify all locale JSONs have zero gaps, and document EmailJS env vars in .env.example.

Purpose: Satisfy FNDTN-03 (all three languages fully populated — phone field translations added) and eliminate the only two hardcoded non-translated strings confirmed by research (AboutPage stub). Phone field addition directly fulfills the CONTEXT.md locked decision that the EmailJS template must capture phone number.

Output: Updated ContactFormSection.tsx (5-field form + emailjs.send with phone), AboutPage.tsx (translated coming-soon), three locale files with phone_label + phone_placeholder keys, .env.example with all VITE_ vars documented.
</objective>

<execution_context>
@/Users/mohamedkhair/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mohamedkhair/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-content-and-qa/06-CONTEXT.md
@.planning/phases/06-content-and-qa/06-RESEARCH.md
@src/components/sections/ContactFormSection.tsx
@src/pages/AboutPage.tsx
@src/i18n/locales/en/en.json
@src/i18n/locales/tr/tr.json
@src/i18n/locales/ar/ar.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add phone field to contact form and locale JSONs</name>
  <files>
    src/components/sections/ContactFormSection.tsx
    src/i18n/locales/en/en.json
    src/i18n/locales/tr/tr.json
    src/i18n/locales/ar/ar.json
  </files>
  <action>
**Step 1 — Add phone translation keys to all three locale JSON files.**

In `src/i18n/locales/en/en.json`, inside `contact.form`, add after `email_label`:
```json
"phone_label": "Phone Number",
"phone_placeholder": "Optional",
```

In `src/i18n/locales/tr/tr.json`, inside `contact.form`, add after `email_label`:
```json
"phone_label": "Telefon Numarası",
"phone_placeholder": "İsteğe bağlı",
```

In `src/i18n/locales/ar/ar.json`, inside `contact.form`, add after `email_label`:
```json
"phone_label": "رقم الهاتف",
"phone_placeholder": "اختياري",
```

**Step 2 — Update `ContactFormSection.tsx`.**

1. Extend `FormValues` interface — add `phone: string` after `email`:
```typescript
interface FormValues {
  name: string;
  email: string;
  phone: string;
  service: string;
  message: string;
}
```

2. Add `phone: ''` to the `useState` initializer alongside the other empty strings.

3. Add `phone: values.phone` to `emailjs.send()` templateParams, after `from_email`:
```typescript
await emailjs.send(
  import.meta.env.VITE_EMAILJS_SERVICE_ID,
  import.meta.env.VITE_EMAILJS_TEMPLATE_ID,
  {
    from_name: values.name,
    from_email: values.email,
    phone: values.phone,
    service: values.service,
    message: values.message,
  },
  { publicKey: import.meta.env.VITE_EMAILJS_PUBLIC_KEY }
);
```

4. Add `phone: ''` to the `setValues` call in the success reset block.

5. Insert the phone input field in the form JSX, between the email field and the service select. Use the same `inputBase` className pattern. Phone is OPTIONAL — do NOT add `required` attribute:
```tsx
<div>
  <label htmlFor="phone" className="mb-1.5 block font-body text-sm font-medium text-stormy-teal-950 dark:text-celadon-100">
    {t('contact.form.phone_label')}
  </label>
  <input
    id="phone"
    name="phone"
    type="tel"
    value={values.phone}
    onChange={handleChange}
    placeholder={t('contact.form.phone_placeholder')}
    className={inputBase}
  />
</div>
```

6. Update the JSDoc comment block at the top of the file to document the fifth template variable:
```
 * - {{phone}}       ← values.phone (optional — not required)
```
  </action>
  <verify>
    <automated>node -e "const c=require('./src/i18n/locales/en/en.json'); console.log(c.contact.form.phone_label, c.contact.form.phone_placeholder)" && node -e "const c=require('./src/i18n/locales/tr/tr.json'); console.log(c.contact.form.phone_label)" && node -e "const c=require('./src/i18n/locales/ar/ar.json'); console.log(c.contact.form.phone_label)" && grep -n "phone: values.phone" src/components/sections/ContactFormSection.tsx && grep -n "type=\"tel\"" src/components/sections/ContactFormSection.tsx</automated>
    <manual>npm run build — must complete with zero TypeScript errors</manual>
  </verify>
  <done>All three locale files contain phone_label and phone_placeholder keys. ContactFormSection.tsx FormValues has phone field, form JSX has tel input, emailjs.send templateParams includes phone: values.phone. npm run build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Fix AboutPage stub and document env vars</name>
  <files>
    src/pages/AboutPage.tsx
    .env.example
    .gitignore
  </files>
  <action>
**Step 1 — Replace hardcoded stub text in AboutPage.tsx.**

Replace the hardcoded `<p>` paragraph:
```tsx
<p className="mt-4 font-body text-seagrass-600 dark:text-seagrass-400">
  Phase 2 stub — content coming in Phase 4
</p>
```

With a translated coming-soon message that uses an i18n key. Add the key `about_coming_soon` to `nav` section in all three locale files first (see below), then use:
```tsx
<p className="mt-4 font-body text-seagrass-600 dark:text-seagrass-400">
  {t('nav.about_coming_soon')}
</p>
```

Add to `src/i18n/locales/en/en.json` → `nav` section:
```json
"about_coming_soon": "This page is coming soon. Visit us on Instagram or contact us for more information."
```

Add to `src/i18n/locales/tr/tr.json` → `nav` section:
```json
"about_coming_soon": "Bu sayfa yakında yayında olacak. Daha fazla bilgi için Instagram'ı ziyaret edin veya bize ulaşın."
```

Add to `src/i18n/locales/ar/ar.json` → `nav` section:
```json
"about_coming_soon": "هذه الصفحة قادمة قريبًا۔ تفضّل بزيارتنا على إنستغرام أو تواصل معنا للمزيد من المعلومات۔"
```

(Arabic uses Arabic full stop ۔ U+06D4 per [Phase 07-02] decision.)

**Step 2 — Create .env.example at project root.**

Create `/path/to/project/.env.example` with placeholder values only (never real credentials):
```bash
# .env.example — commit this file, NOT .env
# Configure real values in:
#   Local dev: copy this file to .env.local and fill in real values
#   Production: Vercel Dashboard → Project → Settings → Environment Variables
#
# EmailJS — https://dashboard.emailjs.com
# 1. Create account at emailjs.com
# 2. Add an Email Service (Gmail, Outlook, custom SMTP)
# 3. Create an Email Template with variables: {{from_name}}, {{from_email}}, {{phone}}, {{service}}, {{message}}
# 4. Copy Service ID, Template ID, and Public Key below
VITE_EMAILJS_SERVICE_ID=your_service_id_here
VITE_EMAILJS_TEMPLATE_ID=your_template_id_here
VITE_EMAILJS_PUBLIC_KEY=your_public_key_here

# WhatsApp Business — E.164 format digits only (no + prefix, no spaces)
# Example: Turkish number +90 555 123 4567 → 905551234567
VITE_WHATSAPP_NUMBER=905XXXXXXXXX
```

**Step 3 — Update .gitignore to ensure .env files are never committed.**

Check if `.gitignore` already covers `.env` patterns. If `*.local` is present (it is, per research), `.env.local` is covered. Add explicit `.env` coverage if not already present. Append to `.gitignore`:
```
# Environment variables — never commit real credentials
.env
.env.*
!.env.example
```

This adds `.env` and `.env.*` exclusion while whitelisting `.env.example` so it can be committed.
  </action>
  <verify>
    <automated>grep -n "about_coming_soon" src/pages/AboutPage.tsx && grep -n "about_coming_soon" src/i18n/locales/en/en.json && grep -n "about_coming_soon" src/i18n/locales/tr/tr.json && grep -n "about_coming_soon" src/i18n/locales/ar/ar.json && test -f .env.example && grep "VITE_EMAILJS_SERVICE_ID" .env.example && grep "\.env\.example" .gitignore</automated>
    <manual>npm run build — zero TypeScript errors. Open AboutPage route in browser — no English dev-stub text visible.</manual>
  </verify>
  <done>AboutPage.tsx renders t('nav.about_coming_soon') — no hardcoded English stub. All three locale files have about_coming_soon key. .env.example exists with all four VITE_ vars documented. .gitignore protects .env while allowing .env.example.</done>
</task>

</tasks>

<verification>
1. Run `node -e "const en=require('./src/i18n/locales/en/en.json'), tr=require('./src/i18n/locales/tr/tr.json'), ar=require('./src/i18n/locales/ar/ar.json'); const ek=JSON.stringify(Object.keys(en)).split(',').length; const tk=JSON.stringify(Object.keys(tr)).split(',').length; const ak=JSON.stringify(Object.keys(ar)).split(',').length; console.log('EN',ek,'TR',tk,'AR',ak)"` — key counts must be equal across all three files.
2. Run `grep -rn "Phase 2 stub\|coming in Phase" src/` — must return zero results.
3. Run `npm run build` — TypeScript must compile without errors.
4. Run `grep "phone: values.phone" src/components/sections/ContactFormSection.tsx` — must match.
5. Run `test -f .env.example && cat .env.example` — must show all four VITE_ vars.
</verification>

<success_criteria>
- Contact form has 5 fields: name, email, phone (optional), service, message
- emailjs.send() passes all 5 fields including phone: values.phone
- AboutPage shows translated coming-soon text in all three languages — zero English stub visible
- All three locale JSONs have identical key counts (phone_label, phone_placeholder, about_coming_soon added)
- .env.example documents setup instructions for EmailJS + WhatsApp env vars
- .gitignore prevents any .env file from being committed (except .env.example)
- npm run build produces zero TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-content-and-qa/06-01-SUMMARY.md`
</output>
