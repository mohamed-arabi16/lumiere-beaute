---
phase: 07-polish-and-ux
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - src/i18n/locales/ar/ar.json
autonomous: true
requirements:
  - POL-02

must_haves:
  truths:
    - "Arabic comma (،) is used in all Arabic list-style text — no Latin commas in Arabic sentences"
    - "كورسات is replaced by دورات throughout ar.json for consistency with academy section"
    - "Em dash (—) in Arabic text is replaced with appropriate Arabic punctuation (colon or space)"
    - "Arabic full stops use Arabic period or natural sentence ending — no Latin period at Arabic sentence ends"
    - "Luxury register vocabulary is consistent — formal Arabic throughout, no colloquialisms"
  artifacts:
    - path: "src/i18n/locales/ar/ar.json"
      provides: "Reviewed and corrected Arabic translations"
      contains: "دورات"
  key_links:
    - from: "src/i18n/locales/ar/ar.json"
      to: "components via useTranslation('common')"
      via: "same key structure, improved values"
      pattern: "JSON keys unchanged"
---

<objective>
Audit and correct `ar.json` for Arabic copy quality — fix punctuation inconsistencies, replace the `كورسات` loanword with `دورات`, correct em dash usage, verify luxury register, and flag remaining items for native-speaker sign-off.

Purpose: POL-02 requires Arabic copy that reads naturally to a native speaker with no machine-translation artifacts, correct gendered forms, and proper RTL punctuation. The research phase identified several specific issues in the current file that can be corrected programmatically.

Output: A corrected `ar/ar.json` with documented fix list. Content changes are limited to values only — all JSON keys remain unchanged so no component updates are needed.
</objective>

<execution_context>
@/Users/mohamedkhair/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mohamedkhair/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-polish-and-ux/07-RESEARCH.md
@src/i18n/locales/ar/ar.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply targeted Arabic copy corrections to ar.json</name>
  <files>src/i18n/locales/ar/ar.json</files>
  <action>
    Read `src/i18n/locales/ar/ar.json` fully, then apply the following specific corrections. Preserve ALL JSON keys exactly — only values change.

    **Correction 1 — كورسات → دورات (loanword consistency):**
    The home section `subtitle` uses `كورسات` (loanword from "courses") while the academy section already uses `دورات` (proper Arabic). Replace every instance of `كورسات` with `دورات` throughout the file for consistency.

    **Correction 2 — Em dash in success_body:**
    Current: `"success_body": "شكرًا لك — سنتواصل معك خلال ٢٤ ساعة."`
    Fix: Replace `—` with `:` (colon is the natural Arabic separator here): `"success_body": "شكرًا لك: سنتواصل معك خلال ٢٤ ساعة."`

    **Correction 3 — Sentence-final Latin periods:**
    Review all string values ending in `.` (Latin period). Arabic sentences should end with `۔` (Arabic full stop, U+06D4) or drop the period entirely for short UI strings. For full sentences (like descriptions and subtitles), replace terminal `.` with `۔`. For short UI labels (like button text or nav labels), remove the trailing period if present. Do NOT change numeric strings like `"٩ ساعات"` — numbers are unaffected.

    **Correction 4 — Verify Arabic comma usage:**
    Scan all values for `,` (Latin comma) used within Arabic prose sentences. Replace any Latin comma used as a list separator in Arabic text with `،` (Arabic comma, U+060C). Do not change commas inside JSON syntax (they are structural, not content).

    **Correction 5 — Luxury register spot-check:**
    Review testimonial `quote` values and treatment `description` values. If any phrase sounds colloquial or overly literal, rewrite for formal luxury register. Key vocabulary to preserve: `فاخرة`, `متجددة`, `مضيئة`, `احترافية`, `معتمدة`. Flag any quote that sounds mechanical in a code comment above the key — use `// REVIEW: [note]` format in the SUMMARY.md, not in the JSON file itself.

    **Correction 6 — Gender-neutral CTAs:**
    Verify CTA imperative verbs use masculine form as gender-neutral standard: `احجز` (book), not `احجزي`. If any CTA uses feminine imperative form, correct to masculine. This is standard practice for gender-mixed audiences.

    IMPORTANT: Do not alter JSON structure, key names, or the nesting hierarchy. Only string values change. After edits, validate the file is valid JSON using `node -e "JSON.parse(require('fs').readFileSync('src/i18n/locales/ar/ar.json', 'utf8'))" 2>&1`.
  </action>
  <verify>
    1. `node -e "JSON.parse(require('fs').readFileSync('src/i18n/locales/ar/ar.json', 'utf8'))"` — exits with no error (valid JSON)
    2. `grep -c "كورسات" src/i18n/locales/ar/ar.json` — returns 0
    3. `grep "دورات" src/i18n/locales/ar/ar.json` — returns at least 2 matches
    4. `grep "\-\-\-" src/i18n/locales/ar/ar.json` — returns 0 (no em dashes in Arabic values)
    5. `npx tsc --noEmit 2>&1 | grep -c error` — returns 0
  </verify>
  <done>ar.json is valid JSON with كورسات replaced by دورات, em dash corrected, terminal periods reviewed, and luxury register maintained.</done>
</task>

</tasks>

<verification>
1. Valid JSON: `node -e "JSON.parse(require('fs').readFileSync('src/i18n/locales/ar/ar.json', 'utf8'))"` — no error
2. Zero `كورسات` remaining: `grep -c "كورسات" src/i18n/locales/ar/ar.json` returns 0
3. TypeScript compiles: `npx tsc --noEmit` — no errors
4. Dev server: Arabic mode renders all pages with corrected copy visible
5. Language switcher: switching TR → EN → AR produces correct copy in each language (confirms other locale files untouched)
</verification>

<success_criteria>
- ar.json is valid JSON (parse check passes)
- كورسات replaced by دورات throughout
- Em dash in success_body corrected to colon
- Sentence-final Latin periods reviewed and corrected where applicable
- Luxury register maintained across testimonials and descriptions
- TypeScript compiles cleanly, dev server loads all three languages without errors
</success_criteria>

<output>
After completion, create `.planning/phases/07-polish-and-ux/07-02-SUMMARY.md` following the summary template. Include a section "Arabic Copy Changes" listing each correction applied and flagging any items requiring native-speaker sign-off.
</output>
